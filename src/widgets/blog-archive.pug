b:includable(id='postPagination')
  .pagination
    b:if(cond='data:newerPageUrl')
      a.newer-link(expr:href='data:newerPageUrl') Newer Posts
    b:if(cond='data:olderPageUrl')
      a.older-link(expr:href='data:olderPageUrl') Older Posts
