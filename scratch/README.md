# Scratch

This directory is intentionally empty and ignored by Git.

Debugging experiments belong here so they never reach a commit. The blank-render investigation
briefly committed `test.html`, `test.pug`, and `test.xml`; they carried no evidence value and were
removed. Anything that does carry evidence belongs in `docs/` with an explanation.
