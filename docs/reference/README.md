# Native Blogger V3 reference

M0 uses an owner-exported Google Contempo theme as the primary platform reference. The source export came from blog `5972841034338492159`, Contempo/Indie template version `1.3.3`, with `b:layoutsVersion='3'` and `b:defaultwidgetversion='2'`.

The full 123,033-byte XML was supplied as `theme-5972841034338492159 (1).xml.txt`. It must be committed locally as `docs/reference/contempo-1.3.3.xml` without formatting or normalization, because the connected GitHub writer cannot transfer a 123 KB attachment byte-for-byte. Do not substitute the truncated preview.
